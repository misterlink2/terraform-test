name: "html"
on:
  push:
   # paths:
    #  - 'html/**'
jobs:
  html:
    name: "html"
    runs-on: ubuntu-latest
    steps:
      - name: html-test
        id: test
        run: echo "in html"

      - name: s3 sync
        id: sync
       # working-directory: $GITHUB_WORKSPACE/html
        run: pwd
        # run: aws s3 sync . s3://test-infra-bucket-austin-1
